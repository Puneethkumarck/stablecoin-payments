apiVersion: v1
kind: Service
metadata:
  name: admin-portal
  namespace: stablebridge
  labels:
    app: admin-portal
spec:
  type: ClusterIP
  selector:
    app: admin-portal
  ports:
    - port: 3001
      targetPort: 3000
      protocol: TCP
      name: http
